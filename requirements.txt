# Python 依賴套件
# 注意：NumPy 必須 < 2.0（與 tflite_runtime 和 OpenCV 相容）
numpy<2.0
soundfile>=0.12.0
pyaudio>=0.2.11
opencv-python>=4.5.0
pyrealsense2>=2.54.0

# TensorFlow Lite Runtime（根據平台選擇）
# x86_64 Linux (Python 3.10):
# 從 https://github.com/google-coral/pycoral/releases 下載
# 或使用：
# pip install https://github.com/google-coral/pycoral/releases/download/v2.0.0/tflite_runtime-2.5.0-cp310-cp310-linux_x86_64.whl

# 其他平台或 Python 版本，請參考：
# https://www.tensorflow.org/lite/guide/python

# ROS2 相關套件由 ROS2 安裝提供，不需在此列出
# 確保已安裝：sudo apt install python3-rosdep python3-colcon-common-extensions

